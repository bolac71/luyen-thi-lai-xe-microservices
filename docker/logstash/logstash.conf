input {
  http {
    port => 5044
    codec => json
  }
}

filter {
  # Optionally parse fields, though nest-winston usually sends JSON
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "microservices-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
